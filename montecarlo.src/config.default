## Use this (without addprocs) to reproduce the exact same results as
## in the paper
# srand(89713564)
@everywhere begin
    ncore = 2
    nsims = 300
    nboot = 199
    RP = [50 250; 150 150; 250 50]
    α = 0.1
end
