## Use this (without addprocs) to reproduce the exact same results as
## in the paper
# srand(89713564)
@everywhere begin
    ncore = 8
    nsims = 500
    nboot = 499
    Ps = [25, 100, 700]
    Rs = [25, 100, 700]
    α = 0.05
end
